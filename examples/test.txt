List of the minimum tests that should be applied when developping new app gateway element:

- new resource creation
    * normal
    * with existing name
    * with/without required parameters
    * with/without optional parameters
    * with/without existing/incorrect referenced params (like probe name)
- resource deletion
- parameters modification
    * addition of some new ones
    * deletion
    * delete all
    * addition from zero conf
- name modification
    * normal (non existing new name)
    * existing name
- manual parameters modification
    * addition
    * deletion
    * modification of existing one
- manual resource deletion

Problems:
- param AffinityCookieName in Backend_http_settings: When set to null initially, azure API don't enable changing it. 
    The solution <as to put a default value (ApplicationGatewayAffinity) as described in the Azure app gateway spec.
- the probe should be created